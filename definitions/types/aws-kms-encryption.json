{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "title": "KMS",
  "description": "AWS KMS encryption configuration",
  "$md.immutable": true,
  "additionalProperties": false,
  "required": [
    "type"
  ],
  "properties": {
    "type": {
      "title": "Type",
      "description": "Select the type of key to use.",
      "type": "string",
      "enum": [
        "default",
        "custom"
      ]
    }
  }
}
